[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -m "not e2e"
markers =
    e2e: end-to-end tests requiring local model services

[tool.pytest.ini_options]
asyncio_mode = "auto"
